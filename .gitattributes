* text=auto eol=lf
# Markdown uses trailing spaces for intentional hard line breaks.
*.md whitespace=-blank-at-eol
# Preserve original evidence bytes and their recorded SHA-256 values.
evidence/** -text -whitespace
